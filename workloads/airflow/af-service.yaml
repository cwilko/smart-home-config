apiVersion: v1
kind: Service
metadata:
  name: airflow-service
  namespace: actions
spec:
  selector:
    app: airflow
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      
